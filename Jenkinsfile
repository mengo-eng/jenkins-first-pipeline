pipeline {
    agent { label 'master'}
    stages {
            stage("build") {
                steps {
                    echo "This time from Github"
		    echo "Now webhook is added to a pipeline"
                    sh 'pwd'
            }
        }
    }
}
