pipeline {
    agent { label 'master'}
    stages {
            stage("build") {
                steps {
                    echo "This time from Github"
                    sh 'echo "Hope it will work this time :) "'
            }
        }
    }
}