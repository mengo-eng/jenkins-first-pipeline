pipeline {
    agent { label 'master'}
    stages {
            stage("build") {
                steps {
                    echo "This time from Github"
                    sh 'pwd'
            }
        }
    }
}